lock-version = "1.0"
environments = ["python_full_version == '3.12.11'"]
created-by = "Financial Manager MS-72"

# The representative service intentionally uses only the Python standard library.
# Consequently the fully resolved third-party package set is empty.
packages = []
