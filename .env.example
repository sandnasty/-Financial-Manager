# Add only non-secret defaults here. Copy to .env for local overrides; .env is ignored.

# Amazon SNS production SMS configuration. Recipient and origination numbers are protected
# runtime values and therefore intentionally omitted.
AWS_REGION=us-west-2
ALERT_SMS_MAX_PRICE_USD=0.05
